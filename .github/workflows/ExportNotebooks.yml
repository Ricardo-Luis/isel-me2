name: Export Pluto notebooks & Deploy
permissions:
    contents: write
on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
    workflow_dispatch:
concurrency:
    group: export
    cancel-in-progress: true

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
            # --- Fase 1: Setup ---
            - name: Checkout repository
              uses: actions/checkout@v4
              with:
                fetch-depth: 0

            - name: Install Julia
              uses: julia-actions/setup-julia@v2
              with:
                version: '1.11'
                arch: x64

            # --- Fase 2: Configuração ---
            - name: Setup environment
              run: |
                  mkdir -p pluto-deployment-environment
                  julia -e '
                  using Pkg
                  Pkg.activate("pluto-deployment-environment")
                  Pkg.add([
                      "Pluto",
                      "PlutoPages",
                      "PlutoUI",
                      "HTTP"
                  ])
                  Pkg.instantiate()
                  Pkg.precompile()
                  '

            # --- Fase 3: Busca de Notebooks (Método Compatível) ---
            - name: Find notebooks
              id: find-notebooks
              run: |
                  # Método compatível para buscar notebooks
                  echo "Notebooks encontrados:"
                  find content/week0 content/data_science -name "*.jl" | tee notebooks.list
                  echo "NOTEBOOKS=$(find content/week0 content/data_science -name "*.jl" | jq -R -s -c 'split("\n")[:-1]')" >> $GITHUB_OUTPUT

            # --- Fase 4: Validação ---
            - name: Validate notebooks
              run: |
                  julia --project=pluto-deployment-environment -e '
                  using Pluto
                  
                  notebooks = ARGS
                  if isempty(notebooks)
                      error("Nenhum notebook encontrado")
                  end
                  
                  for nb in notebooks
                      println("🔍 Validando: ", nb)
                      try
                          Pluto.activate(dirname(nb))
                          Pluto.load_notebook(nb) |> Pluto.update_run!
                          println("✅ Válido")
                      catch e
                          println("❌ Erro: ")
                          showerror(stdout, e)
                          println("\nStacktrace:")
                          Base.show_backtrace(stdout, catch_backtrace())
                          exit(1)
                      end
                  end
                  ' ${{ steps.find-notebooks.outputs.NOTEBOOKS }}

            # --- Fase 5: Geração ---
            - name: Generate site
              run: |
                  ln -sfn content src
                  
                  julia --project=pluto-deployment-environment -e '
                  using PlutoPages
                  
                  try
                      println("⚙️ Iniciando geração...")
                      PlutoPages.generate(".";
                          html_report_path="generation_log.html",
                          notebook_dirs=["week0", "data_science"],
                          fail_on_error=true
                      )
                      
                      if !isdir("_site") || isempty(readdir("_site"))
                          error("Diretório _site vazio ou não criado")
                      end
                      println("✅ Geração concluída")
                  catch e
                      println("❌ Falha na geração:")
                      showerror(stdout, e)
                      exit(1)
                  end
                  '

            # --- Fase 6: Deploy ---
            - name: Prepare and deploy
              run: |
                  mkdir -p www/Fall23
                  
                  if [ -d "_site" ] && [ "$(ls -A _site)" ]; then
                      mv _site/* www/Fall23/
                      echo "🚀 Conteúdo pronto para deploy"
                  else
                      echo "⚠️ Nenhum conteúdo gerado"
                      exit 1
                  fi
                  
                  # Página de redirecionamento
                  cat > www/index.html << 'EOF'
                  <!DOCTYPE html>
                  <html>
                  <head>
                      <meta charset="UTF-8">
                      <meta http-equiv="refresh" content="0; url=./Fall23/">
                  </head>
                  <body>
                      <p>Redirecionando para <a href="./Fall23/">conteúdo do curso</a>...</p>
                  </body>
                  </html>
                  EOF

            - name: Deploy to GitHub Pages
              if: github.event_name == 'push' && github.ref == 'refs/heads/main'
              uses: actions/deploy-pages@v4

            # --- Fase 7: Relatório ---
            - name: Generate report
              if: always()
              run: |
                  echo "# Relatório de Execução" > report.md
                  echo "## Notebooks processados" >> report.md
                  echo '```' >> report.md
                  cat notebooks.list 2>/dev/null || echo "Nenhum notebook processado" >> report.md
                  echo '```' >> report.md
                  
                  if [ -f "generation_log.html" ]; then
                      echo "## Log de Geração" >> report.md
                      echo '```html' >> report.md
                      head -n 50 generation_log.html >> report.md
                      echo '```' >> report.md
                  fi
                  
                  echo "## Ambiente" >> report.md
                  echo '```' >> report.md
                  julia --project=pluto-deployment-environment -e '
                  using Pkg
                  println("Julia ", VERSION)
                  println("Pluto ", Pkg.dependencies()[findfirst(x->x.name=="Pluto", Pkg.dependencies())].version)
                  println("PlutoPages ", Pkg.dependencies()[findfirst(x->x.name=="PlutoPages", Pkg.dependencies())].version)
                  ' >> report.md
                  echo '```' >> report.md
                  
                  echo "REPORT_FILE=report.md" >> $GITHUB_ENV

            - name: Upload report
              uses: actions/upload-artifact@v4
              with:
                name: execution-report
                path: ${{ env.REPORT_FILE }}
                retention-days: 7
